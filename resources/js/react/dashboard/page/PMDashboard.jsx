import React from "react";
import PropTypes from "prop-types";

const PMDashboard = ({ isLoading }) => {
    return <div>PMDashboard</div>;
};

export default PMDashboard;

PMDashboard.propTypes = {
    isLoading: PropTypes.bool,
};
